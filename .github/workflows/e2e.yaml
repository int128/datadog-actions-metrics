name: e2e

on:
  workflow_run:
    workflows:
      - '**'
    types:
      - completed

jobs:
  submit:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Event payload
        run: |
          jq . <<EOF
          ${{ toJSON(github.event) }}
          EOF
      - uses: int128/datadog-actions-metrics@v1
